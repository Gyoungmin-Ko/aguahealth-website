# media-bot 환경 변수
# .env.example을 복사하여 .env 파일을 만들고 값을 채워주세요.
# .env는 .gitignore에 포함되어 있으므로 커밋되지 않습니다.

# OpenAI API (newsletter_gen.py에서 뉴스레터 생성 시 사용)
OPENAI_API_KEY=sk-...

# Resend (send_newsletter.py에서 뉴스레터 발송 시 사용)
RESEND_API_KEY=re_...
# Resend Audiences에서 뉴스레터 구독자 Segment ID (대시보드 → Audiences → Segment 선택 후 URL 또는 설정에서 확인)
RESEND_SEGMENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# 발신 주소/이름 (선택, 미설정 시 onboarding@resend.dev / 아그와헬스)
# NEWSLETTER_FROM_EMAIL=newsletter@agua-health.com
# NEWSLETTER_FROM_NAME=아그와헬스
# 테스트 시 실제 발송 없이 실행: NEWSLETTER_DRY_RUN=1
